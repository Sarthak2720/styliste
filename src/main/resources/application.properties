# Server Config
server.port=8090
#server.servlet.context-path=/api
spring.application.name=styliste

# MySQL Database Config
spring.datasource.url=jdbc:mysql://localhost:3306/styliste_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=RiSa
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate/JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Logging
logging.level.root=INFO
logging.level.com.styliste=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT Configuration
jwt.secret=V++rtkXU/o3lDiqyholA7pqyCDPZpeTgoSP6cYoRB4fwlf9Fq7gdx/EkTK9wZxe6ZDOwgP2UZqzAqL+lJJSbKQ==
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5173
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Jackson Config
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always

# Define where images are stored on your disk
file.upload-dir=uploads/images
# Increase individual file size limit (e.g., 50MB)
spring.servlet.multipart.max-file-size=50MB
# Increase total request size limit (e.g., 50MB)
spring.servlet.multipart.max-request-size=50MB
